### 2. `/front-end/README.md`

```markdown
# Frontend - Sistema de Sorteos (Angular 16)

Este directorio contiene el frontend desarrollado en Angular versión 16 para el sistema de sorteos.

## Requisitos

- Node.js v16 instalado (puedes descargarlo de [nodejs.org](https://nodejs.org))
- npm (incluido con Node.js)
- Angular CLI (opcional, se puede instalar con npm)

## Pasos para instalación y ejecución

1. Abre una terminal y navega a este directorio:

```bash
cd front-end